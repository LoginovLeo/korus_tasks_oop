package OOP;

public class SoundCard extends Motherboard{
    public SoundCard(int id, String producer, String model, String serialNumber, ConnectionType connectionType) {
        super(id, producer, model, serialNumber, connectionType);
    }

}
